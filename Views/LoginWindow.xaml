<Window x:Class="StoreManagementSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:StoreManagementSystem.ViewModels"
        xmlns:helpers="clr-namespace:StoreManagementSystem.Helpers"
        mc:Ignorable="d"
        Title="Store Management System"
     
        Background="#F5F7FA">

    <Window.DataContext>
        <viewModels:LoginViewModel/>
    </Window.DataContext>

    <Grid>

        <Border Width="450"
                Padding="35"
                Background="White"
                CornerRadius="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>

                <DropShadowEffect BlurRadius="20"
                                  ShadowDepth="2"
                                  Opacity="0.15"/>

            </Border.Effect>

            <StackPanel>

                <TextBlock Text="Welcome Back"
                           FontSize="34"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Login to access the system"
                           Foreground="Gray"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0 8 0 35"/>

                <!-- Username -->

                <TextBlock Text="Username"
                           FontWeight="SemiBold"
                           Margin="0 0 0 8"/>

                <TextBox Height="45"
                         FontSize="15"
                         Padding="12"
                         Margin="0 0 0 20"
                         Background="#F8FAFC"
                         BorderBrush="#D1D5DB"
                         Text="{Binding Username,
                         UpdateSourceTrigger=PropertyChanged}" />

                <!-- Password -->

                <TextBlock Text="Password"
                           FontWeight="SemiBold"
                           Margin="0 0 0 8"/>

                <PasswordBox Height="45"
                             FontSize="15"
                             Padding="12"
                             Margin="0 0 0 30"
                             Background="#F8FAFC"
                             BorderBrush="#D1D5DB"
                             helpers:PasswordBoxHelper.BindPassword="True"
                             helpers:PasswordBoxHelper.BoundPassword="{Binding Password,
                             Mode=TwoWay,
                             UpdateSourceTrigger=PropertyChanged}" />

                <!-- Login -->

                <Button Content="LOGIN"
                        Height="50"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Command="{Binding LoginCommand}" />

                <TextBlock Text="Store Management &amp; TPS"
                           Foreground="#9CA3AF"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0 25 0 0"/>

            </StackPanel>

        </Border>

    </Grid>

</Window>